<h1>Questions</h1>

<ul>
  <% @questions.each do |question| %>
    <li><%= link_to question.title, question_path(question) %></li>
  <% end %>
</ul>

<button class="button"><%= link_to "Add a New Question", new_question_path %></button>
